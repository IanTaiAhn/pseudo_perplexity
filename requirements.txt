# Layer 1 — Basic RAG
fastapi==0.111.0
uvicorn[standard]==0.30.1
# Bumped from 2.7.1 -> 2.9.2: langchain 0.3.30 (Layer 4) requires pydantic>=2.7.4.
# Still well within fastapi 0.111's accepted range (<3.0.0), no code changes needed.
pydantic==2.9.2
python-multipart==0.0.9
httpx==0.27.0

# Embeddings
sentence-transformers==3.0.1

# Vector DB
chromadb==0.5.3

# LLM
# Bumped from 0.28.0 -> 0.38.0 for langchain-anthropic (Layer 4) compatibility.
# Pinned at exactly 0.38.0, not latest: anthropic>=0.39.0 drops the bundled
# anthropic/tokenizer.json that ingestion/chunker.py loads at import time via
# importlib.resources — 0.38.0 is the last release that still ships it.
# messages.create() itself is unchanged across this range.
anthropic==0.38.0
accelerate==1.14.0

# Tokenization (for fixed-size chunking)
tiktoken==0.7.0

# PDF parsing
pymupdf==1.24.5

# HTML parsing
beautifulsoup4==4.12.3

# Layer 2 — Web search (httpx already listed above)
# Tavily is called via httpx directly — no extra SDK needed

# Layer 3 — Hybrid search + reranking
rank_bm25==0.2.2
# Reranker (cross-encoder) uses sentence-transformers, already listed above

# Layer 4 — Evaluation pipeline (RAGAS + MLflow)
# ragas 0.2.15 unconditionally imports langchain_community.chat_models.vertexai,
# a module removed in langchain-community>=0.4 (the "sunset" release). Pinning
# langchain-community==0.3.15 (and a compatible langchain/langchain-core/
# langchain-openai trio) is required for `import ragas` to succeed at all —
# see decision log entry 2026-07-07.
ragas==0.2.15
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.15
langchain-openai==0.2.14
# Wraps Claude as the RAGAS judge LLM — keeps eval on the same "no OpenAI" path
# as the rest of the project instead of RAGAS's OpenAI default.
# Pinned at 0.2.4, not latest: newer langchain-anthropic releases require
# anthropic>=0.41.0, which conflicts with the anthropic==0.38.0 pin above
# (needed to keep chunker.py's bundled tokenizer.json working).
langchain-anthropic==0.2.4
# Wraps the project's own sentence-transformers model as the RAGAS judge
# embedder (answer_relevancy), again avoiding an OpenAI embeddings dependency.
langchain-huggingface==0.1.2
# datasets>=3 requires pyarrow>=21, which conflicts with mlflow's pyarrow<19 pin.
datasets==2.21.0
mlflow==2.19.0

# Env
python-dotenv==1.0.1

# Testing and dev tooling
pytest==8.2.2
pytest-asyncio==0.23.7
ruff==0.4.9
mypy==1.10.0